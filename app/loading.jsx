const Loading = () => {
  return (
    <main className="text-center">
      <h2 className="text-primary">Loading...</h2>
      <p>Please wait, it should not take too long</p>
    </main>
  );
};

export default Loading;
